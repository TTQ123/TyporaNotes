为什么需要虚拟DOM
1.框架的意义在于为你掩盖底层的 DOM 操作，让你用更声明式的方式来描述你的目的，从而让你的代码更容易维护。没有任何框架可以比纯手动的优化 DOM 操作更快
2.例如React 的基本思维模式是每次有变动就整个重新渲染整个应用，这就不得不引入虚拟dom，否则每次一更新就需要操作大量真实dom，页面顶不住
3.框架给你的保证是，你在不需要手动优化的情况下，我依然可以给你提供过得去的性能。如果你每一个地方都要手动优化，这是一个多大的工作量，所以框架更多是提升了一些开发的效率。

虚拟dom是一个对象，用普通js对象来描述DOM结构